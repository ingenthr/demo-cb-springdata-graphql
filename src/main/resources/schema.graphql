type Query{
    pokemons: [Pokemon]
    games: [Game]
    game(id: String): Game
}
type Game {
    id: String
    name: String
}
type Pokemon {
    id: String
    game: Game
    name: String
    height: Int
    weight: Int
}